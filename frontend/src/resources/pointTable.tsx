//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const pointTableTitle = () => {
  const record = useRecordContext();
  return <span>pointTable {record ? `"${record.userId}"` : ''}</span>;
};

export const pointTableList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <ReferenceField source="userId" reference="users" />
      <TextField source="state" />
      <NumberField source="totalPoints" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const pointTableEdit = () => (
  <Edit title={<pointTableTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="userId" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="state" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="totalPoints" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const pointTableCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="userId" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="state" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="totalPoints" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  <ReferenceInput source="userId" label="userId" reference="users" alwaysOn />,
  ,
  ,
];
